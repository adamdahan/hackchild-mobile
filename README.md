# hackchild-mobile

Expo (React Native) todo app for the hackchild demo stack.

```bash
npm install
npx expo start
```

Point it at the API with `EXPO_PUBLIC_API_URL`, defaults to `http://localhost:3000`.

Architecture documentation lives in
[hackchild-architecture](https://github.com/adamdahan/hackchild-architecture).
